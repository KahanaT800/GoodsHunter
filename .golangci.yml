run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: goodshunter

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
